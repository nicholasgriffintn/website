{
  "name": "website-blog-api",
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev -c wrangler.json -c ../blog-text-to-speech/wrangler.json -c ../email/wrangler.json -c ../image-resizing/wrangler.json --persist-to ../../.wrangler/state",
    "typecheck": "tsc --noEmit",
    "build": "tsc --project tsconfig.json",
    "deploy": "wrangler deploy src/index.ts"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.47.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "wrangler": "^4.75.0"
  }
}
